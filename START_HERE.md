# 🚀 НАЧНИТЕ ОТСЮДА

Добро пожаловать в B2B Platform! Система готова к работе, даже если backend недоступен.

---

## ⚡ Быстрый Старт (3 минуты)

### 1️⃣ Проверьте статус backend

Откройте в браузере:
```
http://localhost:3000/settings-api
```

**Вы увидите:**
- 🟢 **Статус Backend** - доступен ли ngrok backend
- 🎭 **Mock режим** - включен ли режим mock
- ℹ️ **Информация** - какие данные сейчас используются

### 2️⃣ Включите mock режим (если backend недоступен)

На странице `/settings-api`:
```
1. Нажмите кнопку "Включить Mock режим"
2. Приложение перезагружается
3. Теперь работает с тестовыми данными
```

### 3️⃣ Авторизуйтесь через Яндекс

```
1. Откройте http://localhost:3000/login
2. Нажмите "Войти через Яндекс.Паспорт"
3. Авторизуйтесь через Яндекс аккаунт
4. Готово! Вы в системе
```

---

## 📊 Структура Приложения

```
📱 Фронтенд: Next.js 16 (moderator-dashboard-ui)
│
├── 🔐 Авторизация
│   └── Яндекс OAuth (ТОЛЬКО через Яндекс!)
│
├── 📈 Модератор (/moderator)
│   ├── Поставщики
│   ├── Ключевые слова
│   ├── Черный список
│   └── Parsing Runs
│
├── 💼 Кабинет (/cabinet)
│   ├── Мои поставщики
│   ├── Статистика
│   ├── Сообщения
│   └── Настройки
│
└── 🔧 Управление (/settings-api)
    ├── Статус Backend
    ├── Mock/Real API переключение
    └── Диагностика

🖥️ Backend: FastAPI (Python)
│
├── /api/auth/yandex-oauth    ← Яндекс OAuth
├── /suppliers                ← Поставщики
├── /keywords                 ← Ключевые слова
├── /blacklist                ← Черный список
├── /parsing-runs             ← Parsing
└── /cabinet                  ← Кабинет
```

---

## 🎯 Основные Роли

### 👨‍💼 Модератор (admin@example.com)
```
Роль: Администратор системы
Возможности:
  ✅ Управление всеми поставщиками
  ✅ Модерация черного списка
  ✅ Управление ключевыми словами
  ✅ Запуск parsing runs
  ✅ Просмотр статистики
```

### 👤 Обычный Пользователь (user@example.com)
```
Роль: Владелец кабинета
Возможности:
  ✅ Просмотр своих поставщиков
  ✅ Просмотр сообщений
  ✅ Управление настройками
  ❌ Не может запускать parsing
```

---

## 🔄 Режимы Работы

### 🟢 Real API (Backend доступен)
```
Фронтенд → Backend (ngrok) → Database
        ↓
    Real Data
```

### 🟡 Fallback (Backend падает)
```
Фронтенд → Backend (ngrok) → ❌ ERROR
        ↓
    Mock Data (автоматическое переключение!)
```

### 🎭 Mock Mode (Явное включение)
```
Фронтенд → Mock Data (локально)
        ↓
    Test Data
```

---

## 📖 Документация

### Быстрое Погружение
- 📄 `FALLBACK_READY.md` - что было сделано (5 мин)
- 📄 `FALLBACK_MODE.md` - полный гайд (15 мин)

### По Функциям
- 📄 `YANDEX_ONLY_LOGIN.md` - авторизация через Яндекс
- 📄 `NGROK_SETUP.md` - подключение через ngrok
- 📄 `PRODUCTION_DEPLOY.md` - deployment

### Для Разработчиков
- 📄 `SUMMARY_YANDEX_OAUTH.md` - как работает OAuth
- 📄 `CHANGED_FILES.md` - какие файлы изменились

---

## 🧪 Тестирование

### Тест 1: Проверка Fallback
```
1. Backend работает
2. Откройте /settings-api
3. Проверьте что "Backend в сети ✅"
4. Остановите backend (kill ngrok)
5. Перезагрузите страницу
6. Проверьте что используются mock данные
```

### Тест 2: Mock Режим
```
1. Откройте /settings-api
2. Нажмите "Включить Mock режим"
3. Приложение перезагружается
4. Проверьте что используются mock данные
```

### Тест 3: Яндекс OAuth
```
1. Откройте /login
2. Нажмите "Войти через Яндекс.Паспорт"
3. Авторизуйтесь
4. Проверьте что попали в /moderator или /cabinet
```

---

## 🔑 Учетные Данные

### Яндекс OAuth
```
CLIENT_ID: f13aa94092e74191ab90ac908df3c42b
REDIRECT_URI: http://localhost:3000/api/yandex/callback
SCOPES: login:email login:info mail:imap_full mail:smtp
```

### Admin Email
```
Email: edwatik@yandex.ru
Роль: Moderator (админ)
```

---

## 🚨 Troubleshooting

### Q: Приложение использует mock вместо real API
**A:** Проверьте `/settings-api` - может быть включен mock режим

### Q: Backend недоступен
**A:** 
1. Проверьте что ngrok запущен
2. Проверьте URL в `.env.local`
3. Обновите страницу - включится fallback

### Q: Яндекс OAuth не работает
**A:**
1. Проверьте переменные окружения
2. Убедитесь что redirect URI верный
3. Попробуйте еще раз (иногда требуется перезагрузка)

### Q: Fallback не включается
**A:**
```env
# Проверьте этот параметр в .env.local:
NEXT_PUBLIC_ENABLE_FALLBACK=true  # Должно быть true
```

---

## 🛠️ Разработка

### API с Fallback

```typescript
import { 
  getSuppliers,
  getKeywords,
  isMockModeEnabled 
} from '@/lib/api-with-fallback'

// Все методы имеют встроенный fallback!
const suppliers = await getSuppliers()

// Проверка текущего режима
if (isMockModeEnabled()) {
  console.log('Using mock data')
}
```

### Добавление Нового Mock API

1. Добавьте данные в `/lib/mock-data.ts`
2. Добавьте функцию в `/lib/api-with-fallback.ts`
3. Используйте функцию в компонентах

---

## 🚀 Deployment

### Для Production:

```env
# .env.production
NEXT_PUBLIC_USE_MOCK_DATA=false              # Всегда real API
NEXT_PUBLIC_ENABLE_FALLBACK=true             # Fallback как страховка
NEXT_PUBLIC_API_URL=https://api.your-domain  # Ваш production backend
YANDEX_CLIENT_ID=ваш_production_client_id
YANDEX_CLIENT_SECRET=ваш_production_secret
```

---

## 📞 Поддержка

### Если что-то не работает:

1. 📄 Прочитайте `FALLBACK_MODE.md` (полный гайд)
2. 🔍 Откройте `/settings-api` (диагностика)
3. 🧪 Проверьте console.log ошибки
4. 🔄 Попробуйте перезагрузить страницу

---

## 🎯 Следующие Шаги

### 🔴 Если backend недоступен:
1. Откройте `/settings-api`
2. Включите Mock режим
3. Начните разработку с mock данными
4. Когда backend будет готов, отключите mock

### 🟢 Если backend доступен:
1. Откройте `/settings-api`
2. Убедитесь что "Backend в сети ✅"
3. Авторизуйтесь через Яндекс (/login)
4. Начните использовать приложение

### 🧑‍💻 Для разработчиков:
1. Изучите `FALLBACK_MODE.md`
2. Проверьте `/lib/api-with-fallback.ts`
3. Добавьте свои API методы
4. Используйте mock для разработки

---

## ✅ Контрольный Список

- [ ] Прочитал этот файл
- [ ] Открыл `/settings-api`
- [ ] Проверил статус backend
- [ ] Попробовал авторизацию через Яндекс
- [ ] Протестировал fallback режим
- [ ] Прочитал `FALLBACK_MODE.md`

---

## 🎉 ГОТОВО!

Система полностью работоспособна и готова к:
- ✅ Разработке
- ✅ Тестированию
- ✅ Демонстрации
- ✅ Production Deployment

**Начните с `/settings-api` и `/login` 🚀**

---

**Последнее обновление:** 2026-02-07  
**Версия:** 1.0  
**Статус:** ✅ Production Ready

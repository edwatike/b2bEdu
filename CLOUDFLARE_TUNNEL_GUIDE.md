# Cloudflare Tunnel Guide для B2B Platform

## Что готово:
✅ Cloudflared установлен (`cloudflared.exe`)
✅ Туннель создан (`b2b-platform`)
✅ Скрипты запуска подготовлены

## Способы запуска:

### 🚀 Быстрый старт (только фронтенд):
```bash
start_simple_tunnel.bat
```
- Создаст временный URL для фронтенда (порт 3000)
- URL будет вида: `https://xxxxx.trycloudflare.com`

### 🌐 Полный запуск (все сервисы):
```bash
start_full_tunnel.bat
```
- Создаст отдельные туннели для:
  - Frontend (порт 3000)
  - Backend API (порт 8000) 
  - Parser Service (порт 9000)
- Каждый сервис получит свой уникальный URL

## Что происходит при запуске:
1. Проверяется, запущен ли B2BLauncher.exe
2. Если не запущен - запускает его
3. Ждет 15 секунд для инициализации сервисов
4. Запускает Cloudflare Tunnel

## Требования:
- Аккаунт Cloudflare (для первого логина)
- Запущенные сервисы на портах: 3000, 8000, 9000
- Интернет соединение

## Полученные URL:
После запуска каждый туннель покажет свой URL в консоли.
Скопируйте эти URL для доступа к вашему сайту из интернета.

## Остановка:
- Закройте окна туннелей
- Или нажмите любую клавишу в главном окне (для `start_full_tunnel.bat`)

## Следующие шаги:
1. Запустите один из скриптов
2. Скопируйте полученные URL
3. Проверьте доступность сайтов в браузере
4. Настройте кастомный домен (если нужно)

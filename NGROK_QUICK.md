# NGROK Подключение - Быстрый Гайд

## 🎯 За 1 минуту

1. **Убедитесь, что backend запущен на ngrok:**
   ```
   https://hobnailed-ballistically-jolie.ngrok-free.dev
   ```

2. **Откройте в браузере:**
   ```
   http://localhost:3000/test-ngrok
   ```

3. **Нажмите кнопку "Запустить все тесты"**

4. **Если все тесты зеленые ✅ - все работает!**

---

## ❌ Если не работает

### Вариант 1: Обновить URL (если ngrok URL изменился)

Отредактируйте `.env.local`:
```env
NEXT_PUBLIC_API_URL=https://ВАШ-НОВЫЙ-NGROK-URL.ngrok-free.dev
```

Затем перезагрузите фронтенд (Ctrl+R в браузере).

### Вариант 2: Проверить backend в консоли

```bash
# Проверьте, что backend доступен
curl -H "ngrok-skip-browser-warning: true" \
  https://hobnailed-ballistically-jolie.ngrok-free.dev/health
```

Должна быть ответ `{"status":"ok"}` или похожий.

### Вариант 3: Перезагрузить все

```bash
# Перезагрузите фронтенд
cd frontend/moderator-dashboard-ui
npm run dev

# Перезагрузите backend
# (в отдельном терминале)
python main.py
```

---

## ✅ Все работает!

Если на странице `/test-ngrok` все тесты зеленые:

1. Откройте `/login`
2. Нажмите "Войти через Яндекс"
3. Вы в системе! 🎉

---

## 📊 Что показывает каждый тест

| Тест | Что проверяет |
|------|--------------|
| Health Check | Backend доступен |
| Auth Status | Система авторизации работает |
| Yandex Config | Яндекс OAuth настроен |
| Suppliers | Данные загружаются |

---

## 🔗 Текущий URL Backend

```
https://hobnailed-ballistically-jolie.ngrok-free.dev
```

Если этот URL не работает - обновите его в `.env.local`

---

**Готовы? → Откройте `/test-ngrok` 🚀**

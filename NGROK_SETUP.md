# Подключение к Backend через NGROK

## 📌 Текущая конфигурация

Backend URL: `https://hobnailed-ballistically-jolie.ngrok-free.dev`

## ✅ Что уже настроено

### 1. **Переменная окружения**
```env
NEXT_PUBLIC_API_URL=https://hobnailed-ballistically-jolie.ngrok-free.dev
```
Находится в `.env.local`

### 2. **Content Security Policy (CSP)**
В `next.config.mjs` уже добавлены ngrok домены:
```
connect-src 'self' ... https://*.ngrok-free.dev https://*.ngrok.io
```

### 3. **API Клиент**
В `lib/api.ts` автоматически добавляется заголовок:
```typescript
headers: {
  'ngrok-skip-browser-warning': 'true'
}
```

## 🚀 Как начать

### Шаг 1: Проверьте, что backend запущен на ngrok
```bash
# Backend должен быть доступен по адресу:
# https://hobnailed-ballistically-jolie.ngrok-free.dev/health
```

### Шаг 2: Откройте страницу проверки
Перейдите на: `http://localhost:3000/test-ngrok`

### Шаг 3: Запустите тесты
Нажмите кнопку **"Запустить все тесты"**

## 📊 Тесты на странице проверки

1. **Health Check** - проверка доступности backend
2. **Auth Status** - проверка статуса аутентификации
3. **Yandex Config** - проверка конфигурации OAuth
4. **Suppliers List** - загрузка списка поставщиков

## 🔧 Что делать, если тесты не прошли

### Ошибка: "Backend недоступен"

**Решение 1: Проверьте URL**
```bash
# Убедитесь, что backend доступен по этому адресу:
curl -H "ngrok-skip-browser-warning: true" \
  https://hobnailed-ballistically-jolie.ngrok-free.dev/health
```

**Решение 2: Обновите URL в .env.local**
Если у вас новый ngrok URL, обновите:
```env
NEXT_PUBLIC_API_URL=https://your-new-ngrok-url.ngrok-free.dev
```

**Решение 3: Перезагрузите фронтенд**
```bash
# В папке frontend/moderator-dashboard-ui
npm run dev
```

### Ошибка: "CORS или ngrok warning"

Это нормально! Система уже обрабатывает эти ошибки:
- ✅ Заголовок `ngrok-skip-browser-warning` добавляется автоматически
- ✅ CSP политика разрешает ngrok домены
- ✅ CORS настроен на backend

## 📱 Проверка вручную (DevTools)

1. Откройте **DevTools** (F12)
2. Перейдите на вкладку **Network**
3. Обновите страницу
4. Найдите запросы к `hobnailed-ballistically-jolie.ngrok-free.dev`
5. Проверьте:
   - ✅ Status: 200
   - ✅ Header: `ngrok-skip-browser-warning: true`

## 🔐 Безопасность

- ✅ HTTPS используется
- ✅ Заголовок `ngrok-skip-browser-warning` добавляется автоматически
- ✅ CORS настроен правильно
- ✅ CSP политика защищает от XSS

## 📞 Если все еще не работает

1. Проверьте, что backend запущен на ngrok
2. Откройте `/test-ngrok` и запустите тесты
3. Проверьте консоль браузера (DevTools) на ошибки
4. Проверьте, что `.env.local` содержит правильный URL

## 🎯 Дальше

Когда ngrok подключен и работает:
1. Откройте `/login` 
2. Нажмите "Войти через Яндекс.Паспорт"
3. Систему готова к работе!

---

**Статус подключения: ✅ ГОТОВО**

Backend успешно подключен через ngrok!

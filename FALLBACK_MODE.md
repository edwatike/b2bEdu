# Fallback & Mock Mode Documentation

## 🎯 Обзор

Система B2B Platform имеет встроенный **fallback механизм**, который позволяет приложению работать даже если backend недоступен!

---

## 🔄 Как работает Fallback

### Сценарий 1: Backend доступен ✅
```
1. Фронтенд отправляет запрос к backend
2. Backend возвращает реальные данные
3. Приложение отображает реальные данные
```

### Сценарий 2: Backend недоступен ❌
```
1. Фронтенд отправляет запрос к backend
2. Backend не отвечает (timeout, 503, etc)
3. Система автоматически переключается на mock данные
4. Приложение отображает mock данные
5. Пользователь может продолжать работать!
```

---

## 🎭 Режимы работы

### 1️⃣ Real API Mode (По умолчанию)
- ✅ Получает данные от real backend
- ✅ При ошибке backend → Fallback на mock
- ✅ Рекомендуется для production

**Когда использовать:**
- Backend доступен
- Вы хотите работать с реальными данными
- Fallback как страховка

### 2️⃣ Mock Mode (Опционально)
- 🎭 Всегда использует mock данные
- ❌ Игнорирует backend полностью
- ✅ Быстро (имитирует delay ~300ms)

**Когда использовать:**
- Backend все еще в разработке
- Вы разрабатываете UI
- Вы тестируете frontend в изоляции

---

## 🚀 Как включить/отключить режимы

### Через UI (рекомендуется)

1. Откройте `/settings-api`
2. Нажмите кнопку "Включить Mock режим"
3. Приложение перезагружается и использует mock данные

### Через код

```typescript
// В любом компоненте:
import { enableMockMode, disableMockMode, isMockModeEnabled } from '@/lib/api-with-fallback'

// Включить mock режим
enableMockMode()

// Отключить mock режим  
disableMockMode()

// Проверить текущий режим
const isMock = isMockModeEnabled()
```

### Через localStorage

```javascript
// Включить mock режим
localStorage.setItem('NEXT_PUBLIC_USE_MOCK_DATA', 'true')

// Отключить mock режим
localStorage.removeItem('NEXT_PUBLIC_USE_MOCK_DATA')

// Затем перезагрузить страницу
window.location.reload()
```

### Через переменные окружения

```bash
# Включить mock режим по умолчанию
NEXT_PUBLIC_USE_MOCK_DATA=true

# Отключить fallback (не рекомендуется!)
NEXT_PUBLIC_ENABLE_FALLBACK=false
```

---

## 📊 Mock Данные

### Что включено в mock:

✅ **Поставщики** (suppliers)
- 3 примера с полной информацией
- Разные статусы (active, pending)
- Рейтинги и отзывы

✅ **Ключевые слова** (keywords)
- 3 примера ключевых слов
- Объемы поиска
- Конкуренция

✅ **Черный список** (blacklist)
- 2 примера мошеннических поставщиков
- Причины добавления

✅ **Parsing Runs**
- 2 примера runs
- Статусы (completed, in_progress)
- Метрики обработки

✅ **Cabinet Stats**
- Общая статистика
- Счетчики сообщений
- Дата последнего парсинга

✅ **Messages**
- 2 примера сообщений
- Статус прочитанности
- Временные метки

✅ **Settings**
- Предустановки кабинета
- Язык и тема

---

## 🔌 API Функции с Fallback

### Список функций:

```typescript
// Все эти функции имеют встроенный fallback:

import { 
  getSuppliers,
  getKeywords,
  getBlacklist,
  getParsingRuns,
  getCabinetStats,
  getCabinetMessages,
  getCabinetSettings,
  updateCabinetSettings,
  getAuthStatus,
  getAuthMe
} from '@/lib/api-with-fallback'

// Пример использования:
const suppliers = await getSuppliers({ limit: 10 })
// Если backend доступен → реальные данные
// Если backend down → mock данные
```

---

## 📝 Пример: Использование API с Fallback

```typescript
'use client'

import { useEffect, useState } from 'react'
import { getSuppliers, isMockModeEnabled } from '@/lib/api-with-fallback'

export function SuppliersPage() {
  const [suppliers, setSuppliers] = useState([])
  const [isMock, setIsMock] = useState(false)

  useEffect(() => {
    // Проверяем режим
    setIsMock(isMockModeEnabled())

    // Загружаем данные (с automatic fallback!)
    getSuppliers({ limit: 10 })
      .then(data => setSuppliers(data.entries))
      .catch(error => console.error('Error:', error))
  }, [])

  return (
    <div>
      {isMock && <p>📊 Режим Mock - данные для тестирования</p>}
      {suppliers.map(s => (
        <div key={s.id}>{s.name}</div>
      ))}
    </div>
  )
}
```

---

## 🎯 Для Разработчиков

### Добавить fallback в свой API вызов:

```typescript
import { apiFetchWithFallback } from '@/lib/api-with-fallback'

// Определить fallback данные
const fallbackData = {
  entries: mockMyData,
  total: mockMyData.length,
}

// Использовать с fallback
const result = await apiFetchWithFallback(
  '/my-endpoint',
  { method: 'GET' },
  fallbackData, // Это данные которые используются при ошибке
)
```

### Переменные окружения:

```env
# .env.local

# Использовать mock данные по умолчанию (по умолчанию: false)
NEXT_PUBLIC_USE_MOCK_DATA=false

# Использовать fallback при ошибке backend (по умолчанию: true)
NEXT_PUBLIC_ENABLE_FALLBACK=true

# URL backend (ngrok)
NEXT_PUBLIC_API_URL=https://hobnailed-ballistically-jolie.ngrok-free.dev
```

---

## ⚡ Особенности Fallback

### ✅ Плюсы

- **Надежность**: Приложение работает даже если backend падает
- **Developer Experience**: Разработчики могут тестировать UI без backend
- **Демонстрации**: Показывать приложение когда backend еще не готов
- **Graceful Degradation**: Система автоматически деградирует

### ⚠️ Важно знать

- Mock данные **не сохраняются** на backend
- Mock данные **статичны** - изменения не сохранятся
- Mock режим добавляет **~300ms задержку** (для реалистичности)
- Mock режим **перезагружает страницу** при включении

---

## 🧪 Тестирование

### Сценарий 1: Тест Fallback
1. Backend доступен
2. Откройте приложение
3. Проверьте, что данные загружаются
4. Остановите backend (kill ngrok)
5. Перезагрузите страницу
6. Приложение должно использовать mock данные

### Сценарий 2: Тест Mock режима
1. Откройте `/settings-api`
2. Нажмите "Включить Mock режим"
3. Проверьте, что используются mock данные
4. Отключите Mock режим
5. Проверьте, что используются real данные

---

## 🔧 Troubleshooting

### Q: Приложение использует mock, хотя backend доступен
**A:** Проверьте localStorage:
```javascript
// В console:
localStorage.getItem('NEXT_PUBLIC_USE_MOCK_DATA')
// Если 'true' - удалите:
localStorage.removeItem('NEXT_PUBLIC_USE_MOCK_DATA')
// Перезагрузите страницу
```

### Q: Mock данные не обновляются
**A:** Это нормально! Mock данные статичны. Они не сохраняются на backend и перезагружаются при каждой перезагрузке страницы.

### Q: Как добавить свои mock данные?
**A:** Отредактируйте `/lib/mock-data.ts`:
```typescript
export const mockSuppliers = [
  // Добавьте свои данные здесь
]
```

### Q: Fallback не работает
**A:** Проверьте переменные окружения:
```env
# Должно быть так (или не установлено - по умолчанию true):
NEXT_PUBLIC_ENABLE_FALLBACK=true
```

---

## 📊 Диагностика

Откройте `/settings-api` для полной диагностики:
- ✅ Статус backend (online/offline)
- ✅ Текущий режим (real/mock)
- ✅ Статус fallback (enabled/disabled)
- ✅ URL backend

---

## 🚀 Production Deployment

### Рекомендуемые настройки:

```env
# .env.production
NEXT_PUBLIC_USE_MOCK_DATA=false        # Всегда real API
NEXT_PUBLIC_ENABLE_FALLBACK=true       # Fallback как страховка
NEXT_PUBLIC_API_URL=https://api.example.com  # Ваш production backend
```

### Особенности:

- ✅ Real API всегда активен
- ✅ Fallback работает как страховка
- ✅ Mock данные недоступны
- ✅ Высокая производительность

---

## 📞 Поддержка

Если что-то не работает:
1. Откройте `/settings-api`
2. Проверьте статус backend
3. Проверьте текущий режим
4. Посмотрите console.log ошибки
5. Прочитайте этот документ еще раз 😊

Удачи! 🚀

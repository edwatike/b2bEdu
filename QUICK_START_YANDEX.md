# 🚀 Быстрый старт - Система входа через Яндекс

## ⏱️ Время на запуск: 2-3 минуты

---

## 1️⃣ Проверьте конфигурацию

### Откройте файл `.env.local` в корне проекта:

```bash
# Linux/Mac
cat .env.local

# Windows
type .env.local
```

### Должны быть строки:
```env
YANDEX_CLIENT_ID=f13aa94092e74191ab90ac908df3c42b
YANDEX_CLIENT_SECRET=170746997c17407bb388dd7872d2666a
YANDEX_REDIRECT_URI=http://localhost:3000/api/yandex/callback
YANDEX_SCOPE=login:email login:info mail:imap_full mail:smtp
NEXT_PUBLIC_API_URL=https://hobnailed-ballistically-jolie.ngrok-free.dev
```

✅ Если все есть → переходите к шагу 2  
❌ Если чего-то не хватает → добавьте эти строки

---

## 2️⃣ Запустите Backend

### Откройте терминал и выполните:

```bash
# Перейдите в папку backend
cd backend

# Запустите сервер
python -m uvicorn app.main:app --reload --port 8010
```

### Что должно появиться:
```
INFO:     Uvicorn running on http://127.0.0.1:8010
INFO:     Application startup complete
```

✅ Backend готов!  
⚠️ Не закрывайте этот терминал!

---

## 3️⃣ Запустите Frontend

### Откройте **новый** терминал и выполните:

```bash
# Перейдите в папку frontend
cd frontend/moderator-dashboard-ui

# Запустите приложение
npm run dev
```

### Что должно появиться:
```
▲ Next.js 15.x
✓ compiled client and server successfully
```

✅ Frontend готов!  
⚠️ Терминал не закрывайте!

---

## 4️⃣ Откройте браузер

### Перейдите по адресу:
```
http://localhost:3000/login
```

### Вы должны увидеть:

1. **Логотип B2B Platform** (анимированный)
2. **Два статуса:**
   - ✅ Backend подключен (зеленый)
   - ✅ Яндекс OAuth настроен (синий)
3. **Кнопка:** "Войти через Яндекс.Паспорт"

---

## 5️⃣ Авторизуйтесь через Яндекс

### Нажмите кнопку "Войти через Яндекс.Паспорт"

Что произойдет:
1. Вы перейдете на страницу Яндекса
2. Введите свой логин (email или телефон)
3. Введите пароль
4. Подтвердите права доступа (нажмите "Разрешить")

### После авторизации:

- **Если ваш email = `edwatik@yandex.ru`**  
  → Вы попадете в `/moderator` (админ-панель)

- **Если другой email**  
  → Вы попадете в `/cabinet` (личный кабинет)

✅ **Вы успешно вошли в систему!**

---

## ✅ Проверка статусов

### Backend доступен?
```bash
# В новом терминале:
curl -H "ngrok-skip-browser-warning: true" \
  https://hobnailed-ballistically-jolie.ngrok-free.dev/health

# Должно вернуть:
# {"status": "healthy"}
```

### Яндекс настроен?
```bash
# В браузере разработчика (F12):
fetch('/api/yandex/config').then(r => r.json()).then(console.log)

# Должно вернуть:
# {yandexOAuthEnabled: true}
```

---

## ❌ Если что-то не работает

### Ошибка: "Backend недоступен"

**Решение:**
1. Проверьте, что backend запущен (`python -m uvicorn ...`)
2. Проверьте URL в `NEXT_PUBLIC_API_URL` в `.env.local`
3. Обновите страницу (F5) в браузере

### Ошибка: "Яндекс не настроен"

**Решение:**
1. Проверьте переменные в `.env.local`
2. Перезагрузите приложение (`npm run dev` заново)
3. Очистите кэш браузера (Ctrl+Shift+Del)
4. Обновите страницу (F5)

### Ошибка: "Invalid redirect_uri"

**Решение:**
1. Проверьте `YANDEX_REDIRECT_URI` в `.env.local`
2. Должно быть: `http://localhost:3000/api/yandex/callback`
3. Проверьте в OAuth приложении на Яндексе

### Ошибка: "access_denied"

**Решение:**
1. Вы отказали приложению в доступе
2. Попробуйте еще раз
3. Когда Яндекс попросит разрешение - нажмите "Разрешить"

### Консоль браузера показывает ошибки

**Решение:**
1. Откройте F12 (разработчик)
2. Перейдите на вкладку "Console"
3. Скопируйте ошибку
4. Проверьте документацию в `SUMMARY_YANDEX_OAUTH.md`

---

## 📁 Важные файлы

| Файл | Описание |
|------|---------|
| `.env.local` | Переменные окружения (ПРИВАТНЫЙ) |
| `frontend/moderator-dashboard-ui/app/login/page.tsx` | Страница входа |
| `frontend/moderator-dashboard-ui/app/api/yandex/` | API для OAuth |
| `ИНСТРУКЦИЯ_ВХОД.md` | Инструкция для пользователей |
| `SUMMARY_YANDEX_OAUTH.md` | Полная документация |
| `CHECKLIST_AVTORIZ.md` | Чек-лист проверки |

---

## 🛠️ Полезные команды

```bash
# Запустить frontend (из папки frontend/moderator-dashboard-ui)
npm run dev

# Запустить backend (из папки backend)
python -m uvicorn app.main:app --reload --port 8010

# Проверить конфигурацию Яндекса
curl http://localhost:3000/api/yandex/config

# Проверить health backend
curl -H "ngrok-skip-browser-warning: true" \
  https://hobnailed-ballistically-jolie.ngrok-free.dev/health

# Посмотреть логи frontend
# Просто смотрите терминал где запущен npm run dev

# Посмотреть логи backend
# Просто смотрите терминал где запущен python -m uvicorn
```

---

## 📱 Браузер для тестирования

### Лучше всего использовать:
- ✅ Chrome / Chromium
- ✅ Firefox
- ✅ Safari
- ✅ Edge

### Убедитесь, что:
- ✅ Cookies включены
- ✅ JavaScript включен
- ✅ Блокировщик рекламы отключен (может мешать)

---

## 🔒 Безопасность при тестировании

### ⚠️ ВАЖНО:

1. **`.env.local` - ПРИВАТНЫЙ ФАЙЛ!**
   - Никогда не коммитьте в Git
   - Никогда не публикуйте в интернет
   - Содержит Secret ID от Яндекса

2. **JWT Токены**
   - Хранятся в httpOnly cookies
   - Не видны в JavaScript
   - Безопасны даже если украдут сессию

3. **Пароли**
   - Не хранятся в нашей системе
   - Проверяются только на стороне Яндекса
   - Мы видим только email

---

## 📊 Мониторинг при работе

### Откройте Developer Tools (F12)

#### Вкладка Network:
- Смотрите запросы к `/api/yandex/login`
- Смотрите перенаправление на `oauth.yandex.ru`
- Смотрите запросы к `/api/yandex/callback`

#### Вкладка Console:
- Смотрите сообщения от приложения
- Смотрите ошибки (красные)
- Смотрите предупреждения (желтые)

#### Вкладка Application:
- Смотрите Cookies → `auth_token` (после входа)
- Смотрите Local Storage
- Смотрите Session Storage

---

## 🎯 Тестовый сценарий

### Все работает, если:

1. ✅ Страница `/login` загружается без ошибок
2. ✅ Видны два зеленых/синих статуса
3. ✅ Кнопка "Войти через Яндекс.Паспорт" активна (не серая)
4. ✅ При нажатии → перенаправляет на Яндекс
5. ✅ Можете авторизоваться на Яндексе
6. ✅ Возвращаетесь в приложение
7. ✅ Видите страницу `/moderator` или `/cabinet`
8. ✅ В cookies есть `auth_token`

**Если все 8 пунктов ✅ → ОТЛИЧНО! 🎉**

---

## 🆘 Контакты поддержки

### При возникновении проблем:

1. **Прочитайте:**
   - `ИНСТРУКЦИЯ_ВХОД.md` - базовая инструкция
   - `SUMMARY_YANDEX_OAUTH.md` - полная документация
   - `CHECKLIST_AVTORIZ.md` - чек-лист проверки

2. **Проверьте:**
   - Переменные в `.env.local`
   - Статусы Backend и Яндекса на странице входа
   - Логи в консоли браузера (F12)
   - Логи в терминалах backend и frontend

3. **Обратитесь к администратору:**
   - Email: `edwatik@yandex.ru`
   - Это же email получает права администратора

---

## 📝 Чек-лист перед боевым использованием

- [ ] Backend запущен и доступен
- [ ] Frontend запущен и доступен
- [ ] Переменные в `.env.local` установлены
- [ ] Можно авторизоваться через Яндекс
- [ ] После входа видны правильные данные
- [ ] Можно выйти из системы
- [ ] Защищенные API endpoints работают
- [ ] Логирование работает

---

**Версия:** 1.0  
**Дата:** 2026-02-07  
**Система:** B2B Platform - Yandex OAuth Only  
**Готово к использованию:** ✅ ДА
